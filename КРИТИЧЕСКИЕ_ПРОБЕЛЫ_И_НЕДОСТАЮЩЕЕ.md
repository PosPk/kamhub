# ⚠️ КРИТИЧЕСКИЕ ПРОБЕЛЫ И НЕДОСТАЮЩИЙ ФУНКЦИОНАЛ

> **Дата анализа:** 2025-11-03  
> **Критичность:** HIGH  
> **Статус:** БЛОКИРУЕТ PRODUCTION LAUNCH

---

## 🚨 БЛОКЕРЫ PRODUCTION (Критично!)

### 1. ❌ API КЛЮЧИ ОТСУТСТВУЮТ

**Проблема:**  
ВСЕ API ключи в `.env.local` пустые. Без них система НЕ РАБОТАЕТ.

```bash
# Пустые критичные ключи:
GROQ_API_KEY=                      # ❌ AI чат не работает
DEEPSEEK_API_KEY=                  # ❌ AI fallback не работает
CLOUDPAYMENTS_PUBLIC_ID=           # ❌ Платежи не работают
CLOUDPAYMENTS_API_SECRET=          # ❌ Webhook валидация не работает
YANDEX_MAPS_API_KEY=               # ❌ Карты не работают
YANDEX_WEATHER_API_KEY=            # ❌ Погода не работает
SMTP_USER=                         # ❌ Email уведомления не работают
SMTP_PASS=                         # ❌ Email не отправляются
SMS_RU_API_ID=                     # ❌ SMS не работают
TELEGRAM_BOT_TOKEN=                # ❌ Telegram уведомления не работают
```

**Impact:** 🔴 **CRITICAL**  
Без этих ключей основной функционал не работает!

**Решение:**
```bash
1. Получить API ключи:
   - GROQ: https://console.groq.com (FREE, 14,400 req/day)
   - CloudPayments: https://cloudpayments.ru (регистрация)
   - Yandex Maps: https://developer.tech.yandex.ru
   - SMS.ru: https://sms.ru/api (от 1₽/SMS)
   - SMTP: Настроить Gmail/Yandex SMTP

2. Создать .env.production:
   cp .env.production.example .env.production
   nano .env.production
   # Заполнить ВСЕ ключи

3. Проверить работу:
   npm run dev
   # Протестировать каждый сервис
```

**Статус:** ⏰ **MUST DO перед запуском**

---

### 2. ❌ TOURS API НЕПОЛНЫЙ

**Проблема:**  
Tours API имеет только 4 endpoints, тогда как Transfers API имеет 5.

**Что есть:**
```
✅ GET  /api/tours                  # Список туров
✅ GET  /api/tours/availability     # Проверка доступности
✅ POST /api/tours/book             # Бронирование
✅ POST /api/tours/hold             # Временная блокировка
✅ DELETE /api/tours/hold           # Отмена блокировки
```

**Что ОТСУТСТВУЕТ (критично):**
```
❌ POST /api/tours/search           # Умный поиск туров
❌ POST /api/tours/confirm          # Подтверждение бронирования
❌ POST /api/tours/cancel           # Отмена бронирования
❌ GET  /api/tours/operator/dashboard   # Дашборд оператора
❌ POST /api/tours/payment/confirm  # Webhook от CloudPayments
❌ GET  /api/tours/:id              # Детали тура
❌ GET  /api/tours/schedules        # Расписание туров
❌ POST /api/tours/waitlist         # Добавление в лист ожидания
```

**Impact:** 🔴 **CRITICAL**

**Почему это блокирует production:**
1. **Нет отмены бронирования** - пользователи не могут отменить тур
2. **Нет поиска** - пользователи не могут найти нужный тур
3. **Нет дашборда оператора** - операторы не видят свои туры
4. **Нет webhook для платежей** - платежи не обрабатываются автоматически

**Решение:** Создать недостающие endpoints (2-3 дня работы)

**Статус:** ⏰ **MUST DO**

---

### 3. ❌ NOTIFICATIONS НЕ РЕАЛИЗОВАНЫ

**Проблема:**  
Функция `sendBookingNotifications()` помечена как TODO.

**Код из `/api/tours/book/route.ts`:**
```typescript
// TODO: Реализовать отправку уведомлений
async function sendBookingNotifications(booking: any) {
  // SMS
  // Email
  // Telegram
  // Push notifications
}
```

**Impact:** 🟠 **HIGH**

**Что не работает:**
- ❌ Пользователь НЕ получает подтверждение бронирования
- ❌ Оператор НЕ получает уведомление о новом заказе
- ❌ Нет напоминаний перед туром
- ❌ Нет уведомлений об отмене

**Решение:**
```typescript
// lib/notifications/tour-notifications.ts
import { sendEmail } from './email';
import { sendSMS } from './sms';
import { sendTelegram } from './telegram';

export async function sendBookingConfirmation(booking: TourBooking) {
  const { contactInfo, bookingNumber, totalPrice, scheduleInfo } = booking;
  
  // Email
  await sendEmail({
    to: contactInfo.email,
    subject: `Подтверждение бронирования #${bookingNumber}`,
    html: bookingConfirmationTemplate(booking)
  });
  
  // SMS
  await sendSMS({
    phone: contactInfo.phone,
    text: `Ваше бронирование #${bookingNumber} подтверждено. Код: ${confirmationCode}`
  });
  
  // Telegram (если есть)
  if (booking.telegramId) {
    await sendTelegram({
      chatId: booking.telegramId,
      text: `✅ Бронирование подтверждено!\n📅 ${scheduleInfo.date}\n💰 ${totalPrice}₽`
    });
  }
}

export async function sendOperatorNotification(booking: TourBooking) {
  // Уведомление оператору о новом заказе
}

export async function sendReminderNotification(booking: TourBooking) {
  // Напоминание за 24 часа до тура
}
```

**Статус:** ⏰ **MUST DO** (1 день работы)

---

### 4. ❌ CRON JOB ДЛЯ CLEANUP ОТСУТСТВУЕТ

**Проблема:**  
Функция `cleanupExpiredHolds()` существует, но НИКОГДА не вызывается автоматически.

**Последствия:**
- Истекшие holds НЕ очищаются автоматически
- Места остаются заблокированными навсегда
- База данных засоряется
- Пользователи видят неверную доступность

**Impact:** 🟠 **HIGH**

**Что должно быть:**
```bash
# Cron job запускается каждую минуту
* * * * * curl http://localhost:3000/api/cron/cleanup-holds

# Или через Node.js cron
import cron from 'node-cron';

cron.schedule('* * * * *', async () => {
  console.log('Running cleanup...');
  const cleaned = await cleanupExpiredTourHolds();
  const cleaned2 = await cleanupExpiredTransferHolds();
  console.log(`Cleaned: ${cleaned + cleaned2} holds`);
});
```

**Решение:**

**Вариант 1: API Endpoint + External Cron**
```typescript
// app/api/cron/cleanup-holds/route.ts
import { cleanupExpiredHolds as cleanupTours } from '@/lib/tours/booking';
import { cleanupExpiredHolds as cleanupTransfers } from '@/lib/transfers/booking';

export async function GET(request: NextRequest) {
  // Проверка secret key (защита от внешних вызовов)
  const secret = request.headers.get('X-Cron-Secret');
  if (secret !== process.env.CRON_SECRET) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }
  
  const tours = await cleanupTours();
  const transfers = await cleanupTransfers();
  
  return NextResponse.json({
    success: true,
    cleaned: { tours, transfers, total: tours + transfers }
  });
}
```

**Вариант 2: Node.js встроенный cron**
```typescript
// lib/cron/cleanup-scheduler.ts
import cron from 'node-cron';

export function startCleanupScheduler() {
  // Каждую минуту
  cron.schedule('* * * * *', async () => {
    try {
      const tours = await cleanupExpiredTourHolds();
      const transfers = await cleanupExpiredTransferHolds();
      console.log(`[CLEANUP] Tours: ${tours}, Transfers: ${transfers}`);
    } catch (error) {
      console.error('[CLEANUP ERROR]', error);
    }
  });
  
  console.log('✅ Cleanup scheduler started');
}
```

**Вариант 3: Timeweb Cron (через админку)**
```bash
# В панели Timeweb добавить задание:
# URL: https://your-domain.com/api/cron/cleanup-holds
# Schedule: */1 * * * * (каждую минуту)
# Headers: X-Cron-Secret: your-secret-key
```

**Статус:** ⏰ **MUST DO** (2 часа работы)

---

### 5. ❌ ТЕСТОВ НЕТ

**Проблема:**  
Test coverage 15%, но реальных тестов практически нет.

**Что есть:**
```bash
test/
├── setup.ts              # Конфигурация Vitest
├── race-conditions.test.ts    # Заготовка (не реализовано)
├── booking.test.ts       # Заготовка (не реализовано)
└── webhook-validation.test.ts # Частично реализовано
```

**Impact:** 🟠 **HIGH**

**Что ДОЛЖНО быть протестировано:**

#### Critical (блокирует production):
```typescript
✅ Race Conditions Tests
  - Одновременное бронирование
  - Locks работают корректно
  - NOWAIT возвращает ошибку

✅ Booking Flow Tests
  - Hold → Book → Payment → Confirm
  - Hold истекает корректно
  - Cleanup работает

✅ Payment Webhook Tests
  - HMAC валидация
  - Idempotency
  - Обработка дубликатов

✅ API Validation Tests
  - Zod схемы работают
  - Некорректные данные отклоняются
  - CSRF токены проверяются
```

**Решение:** Написать тесты (3-5 дней)

**Минимум для production:**
```typescript
// test/critical.test.ts
describe('Critical Booking Flow', () => {
  test('should prevent double booking', async () => {
    // Одновременное бронирование одного тура
    const results = await Promise.allSettled([
      bookTour(scheduleId, 5),
      bookTour(scheduleId, 5)
    ]);
    
    // Только одно должно успешно забронироваться
    const successes = results.filter(r => r.status === 'fulfilled');
    expect(successes).toHaveLength(1);
  });
  
  test('hold expires after 15 minutes', async () => {
    const hold = await holdSeats(scheduleId, 2);
    
    // Wait 15 minutes (mocked)
    jest.advanceTimersByTime(15 * 60 * 1000);
    await runCleanup();
    
    // Hold должен быть expired
    const availability = await checkAvailability(scheduleId);
    expect(availability.activeHolds).toBe(0);
  });
});
```

**Статус:** ⏰ **MUST DO перед production**

---

## 🟡 ВАЖНЫЕ (Нужно до полного запуска)

### 6. ⚠️ NODEJS RUNTIME ТОЛЬКО НА 6 ENDPOINTS

**Проблема:**  
Большинство API routes используют edge runtime (медленный).

**Что есть:**
```typescript
// Только 6 endpoints с nodejs runtime:
✅ /api/tours/availability
✅ /api/tours/hold
✅ /api/tours/book
✅ /api/import/asset
✅ /api/ai
✅ /api/health/db
```

**Что НУЖНО добавить:**
```typescript
// Все эти endpoints ДОЛЖНЫ иметь nodejs runtime:
❌ /api/tours (GET list)
❌ /api/transfers/search
❌ /api/transfers/book
❌ /api/transfers/confirm
❌ /api/loyalty/*
❌ /api/webhooks/cloudpayments
❌ /api/operator/*
```

**Impact:** 🟡 **MEDIUM**  
Edge runtime работает, но МЕДЛЕННЕЕ для database операций.

**Решение:**
```typescript
// Добавить в каждый файл route.ts:
export const runtime = 'nodejs';
export const dynamic = 'force-dynamic';
```

**Статус:** 👍 **SHOULD DO** (1 час)

---

### 7. ⚠️ MONITORING НЕ НАСТРОЕН

**Проблема:**  
Sentry DSN пустой, нет error tracking.

```bash
SENTRY_DSN=                        # ❌ Пусто
NEXT_PUBLIC_SENTRY_DSN=            # ❌ Пусто
```

**Impact:** 🟡 **MEDIUM**

**Последствия:**
- Ошибки в production НЕ логируются
- Нет алертов о проблемах
- Невозможно отследить баги
- Нет performance мониторинга

**Решение:**
```bash
1. Зарегистрироваться на Sentry.io (FREE tier)
2. Создать проект
3. Получить DSN
4. Добавить в .env.production:
   SENTRY_DSN=https://xxx@sentry.io/yyy
   NEXT_PUBLIC_SENTRY_DSN=https://xxx@sentry.io/yyy

5. Инициализировать Sentry:
   # sentry.client.config.ts
   Sentry.init({
     dsn: process.env.NEXT_PUBLIC_SENTRY_DSN,
     tracesSampleRate: 0.1,
     environment: process.env.NODE_ENV
   });
```

**Статус:** 👍 **SHOULD DO** (2 часа)

---

### 8. ⚠️ REDIS НЕ НАСТРОЕН

**Проблема:**  
Rate limiting использует in-memory storage (не масштабируется).

```typescript
// lib/middleware/rate-limit.ts
let globalStore: RateLimitStore = new MemoryStore(); // ⚠️ In-memory!
```

**Impact:** 🟡 **MEDIUM**

**Последствия:**
- Rate limits сбрасываются при restart
- Не работает с multiple instances (PM2 cluster)
- Память растет со временем

**Решение:**
```bash
1. Установить Redis:
   # Timeweb Cloud
   # или Docker
   docker run -d -p 6379:6379 redis:alpine

2. Настроить connection:
   REDIS_URL=redis://localhost:6379

3. Использовать RedisStore:
   import Redis from 'ioredis';
   const redis = new Redis(process.env.REDIS_URL);
   setRateLimitStore(new RedisStore(redis));
```

**Статус:** 👍 **SHOULD DO** (3 часа)

---

### 9. ⚠️ BACKUP STRATEGY НЕТ

**Проблема:**  
Есть скрипты `backup-db.sh` и `setup-backup-cron.sh`, но они НЕ НАСТРОЕНЫ.

**Impact:** 🟡 **MEDIUM**

**Что нужно:**
```bash
1. Настроить автоматический backup:
   bash scripts/setup-backup-cron.sh

2. Проверить что работает:
   crontab -l
   # Должен быть backup job

3. Проверить backup файлы:
   ls -lh backups/
   
4. Настроить S3/Timeweb Storage для хранения:
   # Backup retention: 7 daily, 4 weekly, 12 monthly

5. Протестировать restore:
   bash scripts/restore-db.sh backups/latest.sql
```

**Статус:** 👍 **SHOULD DO** (1 день)

---

### 10. ⚠️ TOUR OPERATOR DASHBOARD ОТСУТСТВУЕТ

**Проблема:**  
Есть `/hub/operator/page.tsx`, но нет реального функционала.

**Что должно быть:**
```typescript
/api/tours/operator/dashboard
  - Список моих туров
  - Активные бронирования
  - Статистика (Revenue, Bookings, Cancellations)
  - Календарь расписаний
  - Управление турами (создать, редактировать, удалить)
  - Чекин участников (QR сканер)
  - История отмен
  - Финансовые отчеты
```

**Impact:** 🟢 **LOW** (но важно для операторов)

**Статус:** 📅 **Phase 2**

---

## 🔵 УЛУЧШЕНИЯ (Nice to Have)

### 11. 💡 ОТСУТСТВУЕТ TOUR SEARCH

**Проблема:**  
Есть только GET /api/tours с базовыми фильтрами.

**Что нужно:**
```typescript
POST /api/tours/search
  - Умный поиск по тексту
  - Фильтры (дата, цена, сложность, длительность)
  - Сортировка (популярность, цена, рейтинг)
  - Геопоиск (рядом со мной)
  - Recommendations (AI-powered)
```

**Статус:** 📅 **Phase 2**

---

### 12. 💡 MOBILE APP НЕТ

Мобильное приложение для гидов не разработано.

**Статус:** 📅 **Phase 3**

---

### 13. 💡 ANALYTICS ОТСУТСТВУЕТ

Нет real-time дашбордов с аналитикой.

**Статус:** 📅 **Phase 2**

---

## 📋 ПРИОРИТИЗАЦИЯ

### 🚨 БЛОКЕРЫ (MUST DO перед production):

| # | Задача | Время | Приоритет |
|---|--------|-------|-----------|
| 1 | API ключи | 2 часа | 🔴 CRITICAL |
| 2 | Tours API (cancel, confirm, payment webhook) | 2 дня | 🔴 CRITICAL |
| 3 | Notifications (email, SMS, telegram) | 1 день | 🔴 CRITICAL |
| 4 | Cleanup Cron Job | 2 часа | 🔴 CRITICAL |
| 5 | Critical Tests (race conditions, payment) | 3 дня | 🔴 CRITICAL |

**Итого:** ~7 дней работы

---

### 🟡 ВАЖНЫЕ (Should do):

| # | Задача | Время | Приоритет |
|---|--------|-------|-----------|
| 6 | Nodejs runtime для всех endpoints | 1 час | 🟡 HIGH |
| 7 | Sentry monitoring | 2 часа | 🟡 HIGH |
| 8 | Redis для rate limiting | 3 часа | 🟡 HIGH |
| 9 | Backup strategy | 1 день | 🟡 HIGH |

**Итого:** ~2 дня работы

---

### 🔵 УЛУЧШЕНИЯ (Nice to have):

| # | Задача | Время | Приоритет |
|---|--------|-------|-----------|
| 10 | Tour Operator Dashboard | 3 дня | 🔵 MEDIUM |
| 11 | Smart Tour Search | 2 дня | 🔵 MEDIUM |
| 12 | Analytics Dashboard | 3 дня | 🔵 MEDIUM |
| 13 | Mobile App | 4 недели | 🔵 LOW |

**Итого:** Phase 2-3

---

## ⏱️ TIMELINE ДО PRODUCTION

### Week 1: КРИТИЧЕСКИЕ БЛОКЕРЫ
```
День 1-2: API ключи + Tours API (cancel, confirm, webhook)
День 3: Notifications (SMS, Email, Telegram)
День 4: Cleanup Cron + Tests setup
День 5-7: Critical Tests (race conditions, webhooks, booking flow)
```

### Week 2: ВАЖНЫЕ ДОРАБОТКИ
```
День 1: Nodejs runtime + Sentry
День 2: Redis + Backup strategy
День 3-5: Тестирование и баг-фиксы
```

### Week 3: PRODUCTION LAUNCH
```
День 1: Final testing
День 2: Production deployment
День 3-5: Monitoring и hot-fixes
```

---

## ✅ ЧТО УЖЕ РАБОТАЕТ (Не нужно делать)

✅ **Database Schema** - 30 таблиц, полностью готово  
✅ **Race Condition Protection** - SELECT FOR UPDATE NOWAIT работает  
✅ **Temporary Holds** - 15-минутные блокировки реализованы  
✅ **Booking Logic** - Tours + Transfers booking полностью готов  
✅ **Loyalty System** - 5 уровней, полностью работает  
✅ **Security Middleware** - CSRF, Rate Limiting, Validation готовы  
✅ **Payment Integration** - CloudPayments webhook validation готов  
✅ **Frontend Components** - 12 компонентов готовы  
✅ **Deployment Scripts** - Docker, PM2, Timeweb готовы  

---

## 🎯 ИТОГОВАЯ ОЦЕНКА

**Текущий прогресс:** 85%  
**До production:** 15%  
**Время до готовности:** 2-3 недели  

**Блокеры:** 5 критических задач (~7 дней)  
**Важные:** 4 задачи (~2 дня)  
**Улучшения:** Phase 2-3  

---

## 💡 РЕКОМЕНДАЦИИ

### Immediate Actions (эта неделя):

1. **Получить API ключи** (2 часа)
   ```bash
   - GROQ (бесплатно)
   - CloudPayments (регистрация)
   - Yandex Maps (бесплатно для разработки)
   - SMTP (Gmail или Yandex)
   ```

2. **Создать недостающие Tours API** (2 дня)
   ```bash
   - POST /api/tours/cancel
   - POST /api/tours/confirm
   - POST /api/tours/payment/confirm
   ```

3. **Реализовать notifications** (1 день)
   ```bash
   - Email confirmation
   - SMS confirmation
   - Operator notification
   ```

4. **Настроить cleanup cron** (2 часа)
   ```bash
   - API endpoint /api/cron/cleanup-holds
   - Timeweb Cron или node-cron
   ```

5. **Написать критические тесты** (3 дня)
   ```bash
   - Race condition tests
   - Payment webhook tests
   - Booking flow tests
   ```

### После блокеров:

6. **Настроить мониторинг** (2 часа)
   - Sentry для errors
   - Алерты в Telegram

7. **Настроить Redis** (3 часа)
   - Rate limiting persistence
   - Clustering support

8. **Backup strategy** (1 день)
   - Automated backups
   - S3/Timeweb Storage
   - Restore testing

---

## 📞 NEXT STEPS

### Что делать ПРЯМО СЕЙЧАС:

```bash
# 1. Получить API ключи
# 2. Создать .env.production с реальными ключами
# 3. Создать недостающие API endpoints
# 4. Реализовать notifications
# 5. Настроить cleanup cron
# 6. Написать критические тесты
```

### Checklist перед production:

- [ ] Все API ключи заполнены
- [ ] Tours API полный (cancel, confirm, webhook)
- [ ] Notifications работают (email, SMS)
- [ ] Cleanup cron настроен и работает
- [ ] Критические тесты написаны и проходят
- [ ] Sentry мониторинг настроен
- [ ] Redis для rate limiting настроен
- [ ] Backup strategy работает
- [ ] Security audit пройден
- [ ] Load testing выполнен

---

**🚀 ПОСЛЕ ВЫПОЛНЕНИЯ БЛОКЕРОВ - ГОТОВЫ К PRODUCTION!**

**Автор:** Cursor AI Agent  
**Дата:** 2025-11-03  
**Критичность:** HIGH  
**Требуется action:** IMMEDIATE
