# 📚 СВОДНАЯ ИНСТРУКЦИЯ ПО ДЕПЛОЮ

**Основано на всех инструкциях в репозитории**

---

## 🎯 ОСНОВНЫЕ ВАРИАНТЫ ДЕПЛОЯ

### Вариант 1: Timeweb VDS через GitHub Actions (Рекомендуется) ⭐

**Использует:** SSH подключение к серверу `5.129.248.224`

**Секреты GitHub:**
- `TIMEWEB_TOKEN1` - API токен Timeweb (для поиска сервера)
- `TIMEWEB_SSH_PASSWORD` - SSH пароль (`xQvB1pv?yZTjaR`)
- `TIMEWEB_SSH_USER` - SSH пользователь (`root`)
- `TIMEWEB_SSH_HOST` - IP сервера (`5.129.248.224`) (опционально, если есть TIMEWEB_TOKEN1)

**Workflows:**
- `.github/workflows/timeweb-api-auto.yml` ⚡ - автоматический деплой
- `.github/workflows/timeweb-api-deploy.yml` ⭐ - полный функционал
- `.github/workflows/auto-deploy.yml` 🔄 - с интеграцией API

**Как запустить:**
```bash
git push origin main
```

---

### Вариант 2: Timeweb Cloud Apps (PaaS)

**Использует:** Managed платформу Timeweb (аналог Vercel)

**Приложение:** https://timeweb.cloud/my/apps/125051

**Особенности:**
- Автоматический деплой при git push
- Автоматический SSL
- Managed PostgreSQL
- Встроенный мониторинг

**См. детали:** `TIMEWEB_APPS_ДЕПЛОЙ.md`

---

## 🔐 НЕОБХОДИМЫЕ СЕКРЕТЫ

### Обязательные:

1. **`TIMEWEB_TOKEN1`** ⭐
   - API токен Timeweb
   - Получить: https://timeweb.cloud/my/api
   - Используется для автоматического поиска сервера

2. **`TIMEWEB_SSH_PASSWORD`**
   - SSH пароль: `xQvB1pv?yZTjaR`
   - Используется для подключения к серверу

3. **`TIMEWEB_SSH_USER`** (опционально)
   - По умолчанию: `root`

### Опциональные:

- `TIMEWEB_SSH_HOST` - IP сервера (автоматически находится через API если есть TIMEWEB_TOKEN1)
- `DATABASE_URL` - URL базы данных
- `JWT_SECRET` - секрет для JWT токенов

---

## 🚀 БЫСТРЫЙ СТАРТ

### Шаг 1: Настроить GitHub Secrets

Откройте:
```
https://github.com/PosPk/kamhub/settings/secrets/actions
```

Добавьте:
- `TIMEWEB_TOKEN1` = ваш токен из https://timeweb.cloud/my/api
- `TIMEWEB_SSH_PASSWORD` = `xQvB1pv?yZTjaR`

### Шаг 2: Запустить деплой

**Автоматически (через push):**
```bash
git push origin main
```

**Вручную (через GitHub Actions):**
1. Откройте: https://github.com/PosPk/kamhub/actions
2. Выберите: **⚡ Auto Deploy via Timeweb API**
3. Нажмите: **Run workflow**

---

## 📋 ПРОЦЕСС ДЕПЛОЯ

### Что происходит автоматически:

1. ✅ Workflow получает `TIMEWEB_TOKEN1` из секретов
2. ✅ Запрашивает список серверов через Timeweb API
3. ✅ Находит IP первого доступного сервера (или использует `TIMEWEB_SSH_HOST`)
4. ✅ Подключается через SSH (используя пароль из секретов)
5. ✅ Обновляет код из Git
6. ✅ Устанавливает зависимости (`npm ci`)
7. ✅ Проверяет типы (`npm run type-check`)
8. ✅ Собирает приложение (`npm run build`)
9. ✅ Применяет миграции (`npm run migrate:up`)
10. ✅ Перезапускает через PM2
11. ✅ Проверяет здоровье приложения

---

## 🌐 РЕЗУЛЬТАТ ДЕПЛОЯ

После успешного деплоя:

**Приложение доступно:**
```
http://5.129.248.224:3000
```

**API health check:**
```bash
curl http://5.129.248.224:3000/api/health
```

---

## 🗄️ БАЗА ДАННЫХ

### Вариант 1: Локальный PostgreSQL на сервере

```bash
ssh root@5.129.248.224
apt-get install -y postgresql postgresql-contrib
sudo -u postgres psql << EOF
CREATE DATABASE kamchatour;
CREATE USER kamhub_user WITH PASSWORD 'ваш_пароль';
GRANT ALL PRIVILEGES ON DATABASE kamchatour TO kamhub_user;
EOF
```

### Вариант 2: Timeweb Managed PostgreSQL

Используйте `DATABASE_URL` из панели Timeweb Cloud.

**Добавить в GitHub Secrets:**
- `DATABASE_URL` = `postgresql://user:password@host:5432/database`

---

## ⚙️ НАСТРОЙКА СЕРВЕРА (ПЕРВЫЙ РАЗ)

Если сервер не настроен:

```bash
ssh root@5.129.248.224

# Установка Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs

# Установка PM2
npm install -g pm2
pm2 startup

# Установка PostgreSQL клиента
apt-get install -y postgresql-client

# Создание директории проекта
mkdir -p /var/www/kamhub
```

---

## 📝 ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ

На сервере создайте `.env`:

```bash
nano /var/www/kamhub/.env
```

**Минимальный набор:**
```env
NODE_ENV=production
DATABASE_URL=postgresql://kamhub_user:пароль@localhost:5432/kamchatour
JWT_SECRET=сгенерируйте_через_openssl_rand_-base64_32
PORT=3000
```

**Сгенерировать JWT_SECRET:**
```bash
openssl rand -base64 32
```

---

## 📊 МОНИТОРИНГ

### Проверка статуса:

```bash
ssh root@5.129.248.224
pm2 list
pm2 logs kamchatour-hub
```

### Проверка приложения:

```bash
curl http://localhost:3000/api/health
curl http://5.129.248.224:3000/api/health
```

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### ❌ TIMEWEB_TOKEN1 не найден

**Решение:**
- Проверьте что секрет называется именно `TIMEWEB_TOKEN1`
- Убедитесь что он добавлен в GitHub Secrets

### ❌ Connection failed (SSH)

**Решение:**
- Проверьте `TIMEWEB_SSH_PASSWORD` в секретах
- Убедитесь что пароль правильный: `xQvB1pv?yZTjaR`

### ❌ Server not found through API

**Решение:**
- Workflow автоматически использует fallback на прямой SSH
- Проверьте что `TIMEWEB_SSH_HOST` добавлен (или используется дефолтный `5.129.248.224`)

### ❌ Build failed

**Решение:**
- Смотрите логи в GitHub Actions
- Проверьте ошибки TypeScript
- Проверьте что все зависимости установлены

### ❌ Database connection error

**Решение:**
- Проверьте `DATABASE_URL` в секретах или `.env` на сервере
- Убедитесь что PostgreSQL доступен
- Проверьте права пользователя БД

---

## ✅ ЧЕКЛИСТ ДЕПЛОЯ

### Перед деплоем:
- [ ] GitHub Secrets настроены (`TIMEWEB_TOKEN1`, `TIMEWEB_SSH_PASSWORD`)
- [ ] Workflow файлы созданы
- [ ] Сервер настроен (Node.js, PM2) - опционально (может настраиваться автоматически)

### Деплой:
- [ ] Workflow запущен (через push или вручную)
- [ ] Все шаги выполнены успешно (зеленые в GitHub Actions)
- [ ] Health check пройден

### После деплоя:
- [ ] Приложение доступно по IP
- [ ] API endpoints работают
- [ ] База данных подключена
- [ ] PM2 управляет процессом

---

## 📖 ДОПОЛНИТЕЛЬНАЯ ДОКУМЕНТАЦИЯ

### Основные файлы:
- `ДЕПЛОЙ_TIMEWEB_VDS.md` - полная инструкция по VDS деплою
- `ДЕПЛОЙ_ЧЕРЕЗ_TIMEWEB_TOKEN1.md` - деплой через Timeweb API
- `БЫСТРЫЙ_СТАРТ_ДЕПЛОЯ.md` - быстрый старт
- `TIMEWEB_APPS_ДЕПЛОЙ.md` - деплой на Cloud Apps

### Скрипты:
- `scripts/deploy-timeweb-vds.sh` - основной скрипт деплоя
- `scripts/setup-timeweb-server.sh` - настройка сервера
- `scripts/auto-deploy-all.sh` - всё в одном

### Workflows:
- `.github/workflows/timeweb-api-auto.yml` - автоматический деплой
- `.github/workflows/timeweb-api-deploy.yml` - полный функционал
- `.github/workflows/auto-deploy.yml` - универсальный

---

## 🎉 ГОТОВО!

**Все инструкции собраны в одном месте!**

**Начните с:**
1. Настройки GitHub Secrets (`TIMEWEB_TOKEN1`, `TIMEWEB_SSH_PASSWORD`)
2. Push в main или ручной запуск workflow
3. Проверки результата деплоя

**Всё готово к использованию!** 🚀
