# 🔧 ИСПРАВЛЕНИЕ ОШИБОК ДЕПЛОЯ

**Дата:** $(date)

---

## 🔍 ТИПИЧНЫЕ ОШИБКИ И РЕШЕНИЯ

### ❌ Ошибка 1: TIMEWEB_TOKEN1 not found

**Сообщение:**
```
❌ TIMEWEB_TOKEN1 не найден в GitHub Secrets!
```

**Решение:**
1. Откройте: https://github.com/PosPk/kamhub/settings/secrets/actions
2. Убедитесь что секрет называется именно `TIMEWEB_TOKEN1` (не `TIMEWEB_TOKEN`)
3. Добавьте если отсутствует

---

### ❌ Ошибка 2: Connection failed (SSH)

**Сообщение:**
```
❌ Connection failed
ssh: connect to host ... port 22: Connection refused
```

**Решение:**
1. Проверьте `TIMEWEB_SSH_PASSWORD` в секретах
2. Проверьте что пароль правильный: `xQvB1pv?yZTjaR`
3. Проверьте доступность сервера:
   ```bash
   ping 5.129.248.224
   ```

---

### ❌ Ошибка 3: Server not found through API

**Сообщение:**
```
⚠️ Серверы не найдены через API, используем прямой SSH деплой
```

**Решение:**
- Это не ошибка! Workflow автоматически использует fallback
- Проверьте что `TIMEWEB_SSH_HOST` добавлен в секреты
- Или убедитесь что используется дефолтный IP: `5.129.248.224`

---

### ❌ Ошибка 4: Build failed

**Сообщение:**
```
❌ Build failed
npm ERR! ...
TypeScript errors...
```

**Решение:**
1. Проверьте логи в GitHub Actions
2. Исправьте TypeScript ошибки локально:
   ```bash
   npm run type-check
   ```
3. Закоммитьте исправления и сделайте push

---

### ❌ Ошибка 5: Permission denied

**Сообщение:**
```
Permission denied (publickey,password)
```

**Решение:**
1. Проверьте `TIMEWEB_SSH_PASSWORD` в секретах
2. Убедитесь что пароль правильный
3. Проверьте что `TIMEWEB_SSH_USER` = `root`

---

### ❌ Ошибка 6: Package.json not found

**Сообщение:**
```
❌ package.json не найден!
```

**Решение:**
1. Проверьте что проект клонирован на сервере
2. Убедитесь что Git репозиторий настроен
3. Проверьте путь: `/var/www/kamhub`

---

### ❌ Ошибка 7: Database connection error

**Сообщение:**
```
Error: connect ECONNREFUSED
Database connection failed
```

**Решение:**
1. Проверьте `DATABASE_URL` в секретах или `.env` на сервере
2. Убедитесь что PostgreSQL запущен:
   ```bash
   ssh root@5.129.248.224
   systemctl status postgresql
   ```
3. Проверьте подключение:
   ```bash
   psql $DATABASE_URL -c "SELECT 1;"
   ```

---

### ❌ Ошибка 8: PM2 not found

**Сообщение:**
```
command not found: pm2
```

**Решение:**
1. PM2 должен установиться автоматически
2. Если нет, установите вручную:
   ```bash
   ssh root@5.129.248.224
   npm install -g pm2
   ```

---

## 🔍 КАК НАЙТИ ОШИБКУ

### 1. Откройте GitHub Actions:

```
https://github.com/PosPk/kamhub/actions
```

### 2. Выберите последний workflow run

### 3. Откройте job (например, "🚀 Автоматический деплой")

### 4. Смотрите логи каждого шага:

- Красный шаг = ошибка
- Кликните на шаг чтобы увидеть детали

### 5. Найдите текст ошибки:

Обычно в конце логов есть:
```
Error: ...
❌ ...
Failed: ...
```

---

## 🛠️ БЫСТРЫЕ ИСПРАВЛЕНИЯ

### Если TIMEWEB_TOKEN1 не найден:

```bash
# Проверьте секреты в GitHub
https://github.com/PosPk/kamhub/settings/secrets/actions
```

### Если SSH не работает:

Проверьте что добавлены:
- `TIMEWEB_SSH_PASSWORD` = `xQvB1pv?yZTjaR`
- `TIMEWEB_SSH_USER` = `root` (опционально)

### Если сборка не работает:

Проверьте что в репозитории есть:
- `package.json`
- `package-lock.json`
- Все зависимости указаны

---

## 📋 ПРОВЕРКА ПЕРЕД ПОВТОРНЫМ ДЕПЛОЕМ

- [ ] GitHub Secrets настроены
- [ ] Workflow файлы в репозитории
- [ ] Сервер доступен
- [ ] Нет ошибок в коде (type-check проходит)

---

## 🔄 ПОВТОРНЫЙ ЗАПУСК

После исправления ошибок:

1. **Закоммитьте исправления:**
   ```bash
   git add .
   git commit -m "Fix deployment errors"
   git push origin main
   ```

2. **Или запустите вручную:**
   - Откройте: https://github.com/PosPk/kamhub/actions
   - Выберите workflow
   - Нажмите "Re-run jobs"

---

## 🆘 ЕСЛИ НЕ МОЖЕТЕ НАЙТИ ОШИБКУ

**Пришлите:**
1. Скриншот ошибки из GitHub Actions
2. Или скопируйте текст ошибки из логов
3. Укажите какой шаг упал

**Я помогу исправить!** 🔧
