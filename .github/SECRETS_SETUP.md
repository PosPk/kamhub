# 🔐 НАСТРОЙКА GITHUB SECRETS ДЛЯ ДЕПЛОЯ

## 📋 Как добавить секреты в GitHub

### 1. Перейдите в настройки репозитория:
```
https://github.com/ВАШ_USERNAME/kamhub/settings/secrets/actions
```

### 2. Нажмите "New repository secret"

### 3. Добавьте следующие секреты:

---

## 🔑 ОБЯЗАТЕЛЬНЫЕ СЕКРЕТЫ

### Для SSH деплоя на VDS:

#### `TIMEWEB_SSH_HOST`
```
Значение: 5.129.248.224
```

#### `TIMEWEB_SSH_USER`
```
Значение: root
```

#### `TIMEWEB_SSH_PASSWORD`
```
Значение: xQvB1pv?yZTjaR
```

#### `TIMEWEB_SSH_PORT` (опционально)
```
Значение: 22
(если не указано, используется стандартный порт 22)
```

---

## 🔧 ДОПОЛНИТЕЛЬНЫЕ СЕКРЕТЫ (опционально)

### Для базы данных:

#### `DATABASE_URL` (если нужен удаленный доступ)
```
Значение: postgresql://user:password@host:5432/database
```

### Для JWT:

#### `JWT_SECRET` (для генерации на сервере)
```
Значение: сгенерируйте через:
openssl rand -base64 32
```

### Для AI интеграций:

#### `GROQ_API_KEY`
```
Значение: ваш_ключ_groq
```

#### `DEEPSEEK_API_KEY`
```
Значение: ваш_ключ_deepseek
```

---

## ✅ ПРОВЕРКА

После добавления всех секретов:

1. Убедитесь, что они видны в списке:
   ```
   https://github.com/ВАШ_USERNAME/kamhub/settings/secrets/actions
   ```

2. Проверьте, что workflow файл использует правильные имена:
   ```yaml
   env:
     SSH_HOST: ${{ secrets.TIMEWEB_SSH_HOST }}
     SSH_USER: ${{ secrets.TIMEWEB_SSH_USER }}
     SSH_PASSWORD: ${{ secrets.TIMEWEB_SSH_PASSWORD }}
   ```

3. Запустите workflow вручную или сделайте push:
   ```bash
   git push origin main
   ```

---

## 🔒 БЕЗОПАСНОСТЬ

⚠️ **ВАЖНО:**
- Никогда не коммитьте секреты в код!
- Никогда не показывайте секреты в логах!
- Используйте только GitHub Secrets для хранения паролей
- Регулярно обновляйте пароли и ключи

---

## 📝 ШАБЛОН ДЛЯ БЫСТРОЙ НАСТРОЙКИ

Скопируйте и заполните:

```
TIMEWEB_SSH_HOST=5.129.248.224
TIMEWEB_SSH_USER=root
TIMEWEB_SSH_PASSWORD=xQvB1pv?yZTjaR
TIMEWEB_SSH_PORT=22
```

Добавьте каждый как отдельный секрет в GitHub!
