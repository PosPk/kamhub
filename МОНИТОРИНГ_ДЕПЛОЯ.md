# 📊 МОНИТОРИНГ ДЕПЛОЯ

**Статус:** Workflow запущен! 🔄

---

## 🔍 ЧТО ПРОВЕРЯТЬ

### 1. GitHub Actions

**URL:** https://github.com/PosPk/kamhub/actions

**Что смотреть:**

1. **Последний workflow run** (должен быть желтым 🟡 или зеленым ✅)
2. **Job:** "🚀 Автоматический деплой через Timeweb API"
3. **Шаги выполнения:**

#### ✅ Успешные шаги (зеленые):
- 📥 Checkout code
- 🔧 Setup Node.js
- 📦 Install dependencies
- 🔍 Получение информации о сервере
  - Должен показать IP сервера
- 📦 Install sshpass
- 🚀 Деплой через SSH
- ✅ Health Check
- 📊 Summary

#### ❌ Ошибки (красные):
- Если `TIMEWEB_TOKEN1 not found` → проверьте секреты
- Если `Connection failed` → проверьте SSH пароль
- Если `Server not found` → проверьте API токен

---

## 📋 ЧТО ОЖИДАТЬ В ЛОГАХ

### Шаг 1: Получение информации о сервере

```
🔍 Получение информации о сервере через Timeweb API...
📊 Ответ API:
{
  "servers": [
    {
      "id": "xxx",
      "ip": "5.129.248.224",
      ...
    }
  ]
}
✅ Сервер найден: 5.129.248.224 (ID: xxx)
```

**✅ Хорошо:** IP найден  
**❌ Плохо:** "Серверы не найдены через API"

---

### Шаг 2: SSH деплой

```
🚀 Деплой на root@5.129.248.224:22
🔄 Обновление кода...
📚 Установка зависимостей...
🔍 Проверка типов...
🏗️ Сборка...
🗄️ Миграции...
🔄 Перезапуск...
✅ Приложение перезапущено через PM2
```

**✅ Хорошо:** Все шаги успешны  
**❌ Плохо:** Ошибка на любом шаге

---

### Шаг 3: Health Check

```
🏥 Проверка здоровья...
✅ Health OK
```

**✅ Хорошо:** Health check пройден  
**❌ Плохо:** "Health check failed"

---

## 🎯 ПРОВЕРКА ПОСЛЕ ДЕПЛОЯ

### 1. Проверить логи workflow:

```
https://github.com/PosPk/kamhub/actions
→ Откройте последний run
→ Смотрите логи каждого шага
```

### 2. Найти IP сервера в логах:

В логах должна быть строка:
```
✅ Сервер найден: XXX.XXX.XXX.XXX
```

Или в Summary:
```
🌐 Сервер: XXX.XXX.XXX.XXX
🔗 URL: http://XXX.XXX.XXX.XXX:3000
```

### 3. Проверить приложение:

```bash
# Замените XXX.XXX.XXX.XXX на IP из логов
curl http://XXX.XXX.XXX.XXX:3000/api/health
```

**Ожидаемый ответ:**
```json
{"status":"ok","timestamp":"..."}
```

---

## 🐛 РАСПРОСТРАНЕННЫЕ ПРОБЛЕМЫ

### ❌ TIMEWEB_TOKEN1 не найден

**Решение:**
1. Проверьте что секрет называется именно `TIMEWEB_TOKEN1`
2. Проверьте: https://github.com/PosPk/kamhub/settings/secrets/actions

---

### ❌ Connection failed (SSH)

**Решение:**
1. Проверьте `TIMEWEB_SSH_PASSWORD` в секретах
2. Убедитесь что пароль правильный: `xQvB1pv?yZTjaR`
3. Проверьте что сервер доступен

---

### ❌ Server not found through API

**Решение:**
- Workflow автоматически использует fallback
- Проверьте что `TIMEWEB_SSH_HOST` добавлен в секреты
- Или используется дефолтный: `5.129.248.224`

---

### ❌ Build failed

**Решение:**
1. Смотрите логи шага "🏗️ Сборка..."
2. Проверьте ошибки TypeScript
3. Возможно нужно исправить код

---

### ❌ Migrations failed

**Решение:**
- Это не критично (workflow продолжает работу)
- Проверьте подключение к БД
- Проверьте `DATABASE_URL` в секретах

---

## ✅ УСПЕШНЫЙ ДЕПЛОЙ

### Признаки успеха:

1. ✅ Все шаги зеленые в GitHub Actions
2. ✅ В логах есть: "✅ Деплой завершен!"
3. ✅ Health check пройден
4. ✅ Приложение отвечает на запросы

### Что делать после успешного деплоя:

1. **Сохраните IP сервера** из логов
2. **Проверьте приложение:**
   ```
   http://НАЙДЕННЫЙ_IP:3000
   ```
3. **Проверьте API:**
   ```
   curl http://НАЙДЕННЫЙ_IP:3000/api/health
   ```

---

## 📊 ТЕКУЩИЙ СТАТУС

**Проверьте прямо сейчас:**

```
https://github.com/PosPk/kamhub/actions
```

**Что должно быть:**
- 🟡 Workflow выполняется (желтый)
- ✅ Workflow завершен (зеленый)
- ❌ Workflow завершен с ошибкой (красный)

---

## 🎉 ЕСЛИ ВСЁ УСПЕШНО

После завершения деплоя:

1. ✅ Приложение доступно по IP из логов
2. ✅ API работает: `/api/health`
3. ✅ PM2 управляет процессом
4. ✅ Все миграции применены

**Поздравляем! Деплой успешен!** 🎊

---

**Следите за логами в GitHub Actions!** 📊
