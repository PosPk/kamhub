# 🎨 ВИЗУАЛЬНЫЕ СХЕМЫ И ПРИМЕРЫ РЕАЛИЗАЦИИ

**Дополнение к анализу дизайна**  
**Визуальные решения для многофункционального интерфейса**

---

## 1. 📐 СХЕМА НОВОЙ НАВИГАЦИИ

### 1.1 Desktop Layout (1920px)

```
┌─────────────────────────────────────────────────────────────┐
│  HEADER (Fixed Top)                                         │
│  ┌─────────┬─────────────┬─────────┬──────────┬──────────┐│
│  │ 🏔️ Logo│   Поиск     │🔔 5    │👤 Иван   │⚙️ Settings││
│  └─────────┴─────────────┴─────────┴──────────┴──────────┘│
├──────────┬─────────────────────────────────────────────────┤
│ SIDEBAR  │  MAIN CONTENT                                    │
│ (240px)  │                                                  │
│          │  ┌───────────────────────────────────────────┐  │
│ 🏠 Главн.│  │  Hero Section: Поиск туров                │  │
│          │  │  ┌─────────────┬─────────────┬──────────┐│  │
│ 🏔️ Туры │  │  │ Откуда      │ Куда        │ [Найти]  ││  │
│   └ Кат. │  │  └─────────────┴─────────────┴──────────┘│  │
│   └ Брони│  │                                             │  │
│   └ Избр.│  │  Быстрые действия:                          │  │
│          │  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐    │  │
│ 🚌 Транс.│  │  │ Попу-│ │ Транс-│ │ Погода│ │ AI   │    │  │
│   └ Поиск│  │  │ лярн.│ │ феры │ │   🌤️ │ │ 🤖  │    │  │
│   └ Заказы│  │  └──────┘ └──────┘ └──────┘ └──────┘    │  │
│          │  │                                             │  │
│ 🤖 AI    │  │  Результаты поиска:                         │  │
│          │  │  [Grid с карточками туров]                  │  │
│ 🌤️ Погод.│  │                                                  │  │
│          │  └───────────────────────────────────────────┘  │
│ ⚙️ Настр.│                                                  │
│          │                                                  │
└──────────┴─────────────────────────────────────────────────┘
```

### 1.2 Mobile Layout (375px)

```
┌─────────────────────────┐
│  HEADER (Sticky)        │
│  🏔️ Logo    🔔 👤      │
├─────────────────────────┤
│                         │
│  MAIN CONTENT           │
│                         │
│  [Hero Search]          │
│                         │
│  [Quick Actions]        │
│                         │
│  [Results]              │
│                         │
│                         │
├─────────────────────────┤
│ BOTTOM NAV (Fixed)      │
│ 🏔️ 🚌 🤖 ❤️ ⚙️         │
└─────────────────────────┘
```

---

## 2. 🎯 КОНКРЕТНЫЕ UI РЕШЕНИЯ

### 2.1 Sidebar Navigation Component

```tsx
// components/Layout/Sidebar.tsx
'use client';

import { usePathname } from 'next/navigation';
import Link from 'next/link';
import { useRoles } from '@/contexts/RoleContext';

interface NavItem {
  id: string;
  label: string;
  icon: string;
  href: string;
  badge?: number;
  children?: NavItem[];
}

const getNavigationForRole = (role: string): NavItem[] => {
  const base = [
    {
      id: 'home',
      label: 'Главная',
      icon: '🏠',
      href: '/',
    },
    {
      id: 'tours',
      label: 'Туры',
      icon: '🏔️',
      href: '/tours',
      children: [
        { id: 'tours-catalog', label: 'Каталог', href: '/tours' },
        { id: 'tours-bookings', label: 'Мои бронирования', href: '/tours/bookings' },
        { id: 'tours-favorites', label: 'Избранное', href: '/tours/favorites' },
      ],
    },
    {
      id: 'transfers',
      label: 'Трансферы',
      icon: '🚌',
      href: '/transfers',
      children: [
        { id: 'transfers-search', label: 'Поиск', href: '/transfers' },
        { id: 'transfers-orders', label: 'Мои заказы', href: '/transfers/orders' },
      ],
    },
  ];

  if (role === 'operator') {
    return [
      ...base,
      {
        id: 'operator',
        label: 'CRM',
        icon: '📊',
        href: '/operator',
        children: [
          { id: 'operator-dashboard', label: 'Дашборд', href: '/operator/dashboard' },
          { id: 'operator-tours', label: 'Туры', href: '/operator/tours' },
          { id: 'operator-bookings', label: 'Бронирования', href: '/operator/bookings' },
          { id: 'operator-analytics', label: 'Аналитика', href: '/operator/analytics' },
        ],
      },
    ];
  }

  return base;
};

export function Sidebar() {
  const pathname = usePathname();
  const { roles } = useRoles();
  const navigation = getNavigationForRole(roles[0] || 'traveler');
  const [expandedItems, setExpandedItems] = useState<string[]>([]);

  const toggleExpand = (id: string) => {
    setExpandedItems(prev =>
      prev.includes(id) ? prev.filter(i => i !== id) : [...prev, id]
    );
  };

  return (
    <aside className="fixed left-0 top-0 h-screen w-64 bg-premium-black border-r border-premium-gold/20 z-40">
      <div className="p-4 border-b border-premium-gold/20">
        <div className="flex items-center gap-2">
          <div className="w-8 h-8 rounded-lg bg-gold-gradient" />
          <span className="font-bold text-premium-gold">Kamchatour</span>
        </div>
      </div>

      <nav className="p-4 space-y-1 overflow-y-auto h-[calc(100vh-80px)]">
        {navigation.map(item => (
          <div key={item.id}>
            {item.children ? (
              <>
                <button
                  onClick={() => toggleExpand(item.id)}
                  className={`w-full flex items-center justify-between px-3 py-2 rounded-lg transition-colors ${
                    pathname.startsWith(item.href)
                      ? 'bg-premium-gold/20 text-premium-gold'
                      : 'text-white/70 hover:bg-white/5 hover:text-white'
                  }`}
                >
                  <div className="flex items-center gap-3">
                    <span>{item.icon}</span>
                    <span className="font-medium">{item.label}</span>
                  </div>
                  <span className={expandedItems.includes(item.id) ? 'rotate-180' : ''}>
                    ▼
                  </span>
                </button>
                {expandedItems.includes(item.id) && (
                  <div className="ml-8 mt-1 space-y-1">
                    {item.children.map(child => (
                      <Link
                        key={child.id}
                        href={child.href}
                        className={`block px-3 py-2 rounded-lg text-sm transition-colors ${
                          pathname === child.href
                            ? 'text-premium-gold bg-premium-gold/10'
                            : 'text-white/60 hover:text-white hover:bg-white/5'
                        }`}
                      >
                        {child.label}
                      </Link>
                    ))}
                  </div>
                )}
              </>
            ) : (
              <Link
                href={item.href}
                className={`flex items-center gap-3 px-3 py-2 rounded-lg transition-colors ${
                  pathname === item.href
                    ? 'bg-premium-gold/20 text-premium-gold'
                    : 'text-white/70 hover:bg-white/5 hover:text-white'
                }`}
              >
                <span>{item.icon}</span>
                <span className="font-medium">{item.label}</span>
                {item.badge && (
                  <span className="ml-auto px-2 py-0.5 bg-premium-gold text-premium-black text-xs rounded-full">
                    {item.badge}
                  </span>
                )}
              </Link>
            )}
          </div>
        ))}
      </nav>
    </aside>
  );
}
```

### 2.2 Упрощенная главная страница

```tsx
// app/page.tsx (улучшенная версия)
'use client';

import { Sidebar } from '@/components/Layout/Sidebar';
import { Header } from '@/components/Layout/Header';
import { QuickActions } from '@/components/Dashboard/QuickActions';

export default function Home() {
  return (
    <div className="flex min-h-screen bg-premium-black">
      <Sidebar />
      
      <main className="flex-1 ml-64">
        <Header />
        
        {/* Hero Section */}
        <section className="relative overflow-hidden">
          <div className="absolute inset-0 gradient-gold-aurora animate-aurora" />
          <div className="relative max-w-6xl mx-auto px-6 py-16">
            <h1 className="text-5xl font-black text-white mb-4">
              Откройте <span className="text-premium-gold">Камчатку</span>
            </h1>
            <p className="text-xl text-white/80 mb-8">
              Туры, трансферы, безопасность и экология в одной платформе
            </p>
            
            {/* Universal Search */}
            <div className="bg-white/5 backdrop-blur-lg rounded-2xl p-6 border border-white/10">
              <div className="grid md:grid-cols-3 gap-4 mb-4">
                <input
                  type="text"
                  placeholder="Откуда"
                  className="input-premium"
                />
                <input
                  type="text"
                  placeholder="Куда"
                  className="input-premium"
                />
                <input
                  type="date"
                  className="input-premium"
                />
              </div>
              <button className="button-primary w-full">
                🔍 Найти туры и трансферы
              </button>
            </div>
          </div>
        </section>

        {/* Quick Actions */}
        <section className="max-w-6xl mx-auto px-6 py-12">
          <h2 className="text-2xl font-bold text-white mb-6">Быстрые действия</h2>
          <QuickActions />
        </section>

        {/* Recommendations */}
        <section className="max-w-6xl mx-auto px-6 pb-12">
          <h2 className="text-2xl font-bold text-white mb-6">Рекомендуем для вас</h2>
          {/* Tour cards grid */}
        </section>
      </main>
    </div>
  );
}
```

### 2.3 Quick Actions Component

```tsx
// components/Dashboard/QuickActions.tsx
'use client';

export function QuickActions() {
  const actions = [
    {
      id: 'popular-tours',
      title: 'Популярные туры',
      icon: '🏔️',
      href: '/tours?sort=popular',
      color: 'from-blue-500/20 to-purple-500/20',
    },
    {
      id: 'transfers',
      title: 'Трансферы',
      icon: '🚌',
      href: '/transfers',
      color: 'from-green-500/20 to-teal-500/20',
    },
    {
      id: 'weather',
      title: 'Погода',
      icon: '🌤️',
      href: '/weather',
      color: 'from-yellow-500/20 to-orange-500/20',
    },
    {
      id: 'ai-assistant',
      title: 'AI-помощник',
      icon: '🤖',
      href: '/ai-chat',
      color: 'from-purple-500/20 to-pink-500/20',
    },
  ];

  return (
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
      {actions.map(action => (
        <Link
          key={action.id}
          href={action.href}
          className={`card-premium hover-lift bg-gradient-to-br ${action.color} group`}
        >
          <div className="text-4xl mb-3 group-hover:scale-110 transition-transform">
            {action.icon}
          </div>
          <h3 className="text-lg font-bold text-white mb-2">{action.title}</h3>
          <p className="text-white/60 text-sm">Перейти →</p>
        </Link>
      ))}
    </div>
  );
}
```

### 2.4 Mobile Bottom Navigation

```tsx
// components/Navigation/MobileNav.tsx
'use client';

import { usePathname } from 'next/navigation';
import Link from 'next/link';

const navItems = [
  { id: 'home', label: 'Главная', icon: '🏠', href: '/' },
  { id: 'tours', label: 'Туры', icon: '🏔️', href: '/tours' },
  { id: 'transfers', label: 'Трансферы', icon: '🚌', href: '/transfers' },
  { id: 'ai', label: 'AI', icon: '🤖', href: '/ai-chat' },
  { id: 'profile', label: 'Профиль', icon: '👤', href: '/profile' },
];

export function MobileNav() {
  const pathname = usePathname();

  return (
    <nav className="fixed bottom-0 left-0 right-0 bg-premium-black border-t border-premium-gold/20 md:hidden z-50">
      <div className="flex justify-around items-center h-16">
        {navItems.map(item => {
          const isActive = pathname === item.href;
          
          return (
            <Link
              key={item.id}
              href={item.href}
              className={`flex flex-col items-center justify-center flex-1 h-full transition-colors ${
                isActive ? 'text-premium-gold' : 'text-white/60'
              }`}
            >
              <span className="text-2xl mb-1">{item.icon}</span>
              <span className="text-xs font-medium">{item.label}</span>
              {isActive && (
                <div className="absolute top-0 left-0 right-0 h-1 bg-premium-gold" />
              )}
            </Link>
          );
        })}
      </div>
    </nav>
  );
}
```

---

## 3. 📱 АДАПТИВНЫЕ РЕШЕНИЯ

### 3.1 Responsive Breakpoints Strategy

```tsx
// Tailwind breakpoints
sm: '640px'   // Mobile landscape
md: '768px'   // Tablet
lg: '1024px'  // Desktop
xl: '1280px'  // Large desktop
2xl: '1536px' // Extra large

// Layout decisions:
// < 768px: Bottom Navigation + Collapsed Sidebar
// >= 768px: Sidebar (collapsed) + Top Navigation
// >= 1024px: Full Sidebar + Top Navigation
```

### 3.2 Collapsible Sidebar

```tsx
// components/Layout/Sidebar.tsx
export function Sidebar() {
  const [isCollapsed, setIsCollapsed] = useState(false);
  const isMobile = useMediaQuery('(max-width: 768px)');

  if (isMobile) {
    return <MobileNav />;
  }

  return (
    <aside className={`
      fixed left-0 top-0 h-screen transition-all duration-300 z-40
      ${isCollapsed ? 'w-16' : 'w-64'}
      bg-premium-black border-r border-premium-gold/20
    `}>
      {/* Toggle button */}
      <button
        onClick={() => setIsCollapsed(!isCollapsed)}
        className="absolute -right-3 top-4 w-6 h-6 rounded-full bg-premium-gold text-premium-black flex items-center justify-center"
      >
        {isCollapsed ? '→' : '←'}
      </button>

      {/* Navigation content */}
      <nav className={isCollapsed ? 'p-2' : 'p-4'}>
        {/* Items with icons only when collapsed */}
      </nav>
    </aside>
  );
}
```

---

## 4. 🎨 УЛУЧШЕНИЕ ДАШБОРДОВ

### 4.1 Operator Dashboard (упрощенный)

```tsx
// app/operator/dashboard/page.tsx
export default function OperatorDashboard() {
  return (
    <AppLayout>
      {/* Overview Cards */}
      <div className="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
        <StatCard
          label="Всего туров"
          value={stats.totalTours}
          icon="🏔️"
          trend={+12}
        />
        <StatCard
          label="Активные бронирования"
          value={stats.activeBookings}
          icon="📅"
          trend={+5}
        />
        <StatCard
          label="Доход за месяц"
          value={`${stats.monthlyRevenue.toLocaleString()}₽`}
          icon="💰"
          trend={+23}
        />
        <StatCard
          label="Рейтинг"
          value={stats.rating}
          icon="⭐"
        />
      </div>

      {/* Two Column Layout */}
      <div className="grid lg:grid-cols-3 gap-6">
        {/* Main Content */}
        <div className="lg:col-span-2 space-y-6">
          {/* Recent Bookings Table */}
          <Card>
            <CardHeader>
              <h3>Последние бронирования</h3>
              <Link href="/operator/bookings">Смотреть все →</Link>
            </CardHeader>
            <BookingsTable bookings={recentBookings} />
          </Card>
        </div>

        {/* Sidebar */}
        <div className="space-y-6">
          {/* Weather Alert */}
          {weather.safetyLevel !== 'excellent' && (
            <Alert type="warning">
              Погодное предупреждение
            </Alert>
          )}

          {/* Quick Actions */}
          <Card>
            <CardHeader>
              <h3>Быстрые действия</h3>
            </CardHeader>
            <div className="space-y-2">
              <Button href="/operator/tours/new">+ Создать тур</Button>
              <Button variant="secondary" href="/operator/analytics">
                📊 Аналитика
              </Button>
            </div>
          </Card>
        </div>
      </div>
    </AppLayout>
  );
}
```

---

## 5. 🔄 УЛУЧШЕННЫЕ ПОТОКИ

### 5.1 Поиск → Бронирование (One-Page Flow)

```tsx
// app/tours/page.tsx - улучшенный поиск с inline бронированием
export default function ToursPage() {
  const [selectedTour, setSelectedTour] = useState<Tour | null>(null);
  const [showBookingModal, setShowBookingModal] = useState(false);

  return (
    <AppLayout>
      {/* Search Bar (Sticky) */}
      <div className="sticky top-0 z-10 bg-premium-black/95 backdrop-blur-lg border-b border-white/10">
        <TourSearch onResults={setTours} />
      </div>

      {/* Results Grid */}
      <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6 p-6">
        {tours.map(tour => (
          <TourCard
            key={tour.id}
            tour={tour}
            onBook={() => {
              setSelectedTour(tour);
              setShowBookingModal(true);
            }}
          />
        ))}
      </div>

      {/* Booking Modal (Slide-over справа) */}
      {showBookingModal && selectedTour && (
        <SlideOver
          isOpen={showBookingModal}
          onClose={() => setShowBookingModal(false)}
          title="Бронирование тура"
        >
          <BookingForm tour={selectedTour} />
        </SlideOver>
      )}
    </AppLayout>
  );
}
```

### 5.2 Slide-over Component

```tsx
// components/UI/SlideOver.tsx
'use client';

import { useEffect } from 'react';
import { createPortal } from 'react-dom';

export function SlideOver({
  isOpen,
  onClose,
  title,
  children,
}: {
  isOpen: boolean;
  onClose: () => void;
  title: string;
  children: React.ReactNode;
}) {
  useEffect(() => {
    if (isOpen) {
      document.body.style.overflow = 'hidden';
    } else {
      document.body.style.overflow = '';
    }
  }, [isOpen]);

  if (!isOpen) return null;

  return createPortal(
    <div className="fixed inset-0 z-50">
      {/* Backdrop */}
      <div
        className="absolute inset-0 bg-black/50 backdrop-blur-sm"
        onClick={onClose}
      />

      {/* Slide-over Panel */}
      <div className="absolute right-0 top-0 h-full w-full md:w-96 bg-premium-black border-l border-premium-gold/20 animate-slide-in-right">
        <div className="h-full flex flex-col">
          {/* Header */}
          <div className="p-6 border-b border-white/10 flex items-center justify-between">
            <h2 className="text-xl font-bold text-white">{title}</h2>
            <button
              onClick={onClose}
              className="text-white/70 hover:text-white"
            >
              ✕
            </button>
          </div>

          {/* Content */}
          <div className="flex-1 overflow-y-auto p-6">
            {children}
          </div>
        </div>
      </div>
    </div>,
    document.body
  );
}
```

---

## 6. 💡 ИННОВАЦИОННЫЕ ФИЧИ

### 6.1 Command Palette (⌘K)

```tsx
// components/CommandPalette/CommandPalette.tsx
'use client';

import { useEffect, useState } from 'react';
import { useRouter } from 'next/navigation';

export function CommandPalette() {
  const [isOpen, setIsOpen] = useState(false);
  const [query, setQuery] = useState('');
  const router = useRouter();

  const commands = [
    { id: 'tour-search', label: 'Найти туры', icon: '🏔️', action: () => router.push('/tours') },
    { id: 'transfer-search', label: 'Найти трансфер', icon: '🚌', action: () => router.push('/transfers') },
    { id: 'ai-chat', label: 'Открыть AI-помощника', icon: '🤖', action: () => router.push('/ai-chat') },
    { id: 'bookings', label: 'Мои бронирования', icon: '📅', action: () => router.push('/bookings') },
  ];

  useEffect(() => {
    const handler = (e: KeyboardEvent) => {
      if (e.key === 'k' && (e.metaKey || e.ctrlKey)) {
        e.preventDefault();
        setIsOpen(true);
      }
      if (e.key === 'Escape') {
        setIsOpen(false);
      }
    };

    window.addEventListener('keydown', handler);
    return () => window.removeEventListener('keydown', handler);
  }, []);

  const filteredCommands = commands.filter(cmd =>
    cmd.label.toLowerCase().includes(query.toLowerCase())
  );

  if (!isOpen) return null;

  return (
    <div className="fixed inset-0 z-50 flex items-start justify-center pt-[20vh]">
      <div className="bg-premium-black border border-premium-gold/30 rounded-2xl shadow-2xl w-full max-w-lg p-4">
        <input
          type="text"
          placeholder="Введите команду... (⌘K)"
          value={query}
          onChange={(e) => setQuery(e.target.value)}
          className="w-full bg-white/5 border border-white/10 rounded-lg px-4 py-3 text-white mb-4"
          autoFocus
        />
        <div className="space-y-1">
          {filteredCommands.map(cmd => (
            <button
              key={cmd.id}
              onClick={() => {
                cmd.action();
                setIsOpen(false);
              }}
              className="w-full flex items-center gap-3 px-4 py-3 rounded-lg hover:bg-white/5 text-left"
            >
              <span>{cmd.icon}</span>
              <span className="text-white">{cmd.label}</span>
            </button>
          ))}
        </div>
      </div>
    </div>
  );
}
```

---

## 7. 📊 СРАВНЕНИЕ: БЫЛО → СТАЛО

### Было:
```
❌ 5-6 вкладок на дашборде
❌ Переходы между страницами для каждой функции
❌ Не понятно где что находится
❌ Мобильная версия неудобна
```

### Стало:
```
✅ Sidebar с иерархической навигацией
✅ Отдельные страницы для основных функций
✅ Быстрые действия на главной
✅ Inline редактирование/бронирование
✅ Command Palette для быстрого доступа
✅ Адаптивная навигация (Sidebar/Bottom Nav)
```

---

## 8. 🎯 ПРИОРИТЕТЫ РЕАЛИЗАЦИИ

### Фаза 1 (Неделя 1-2): Основа
- [ ] Создать AppLayout с Sidebar
- [ ] Мигрировать главную страницу
- [ ] Добавить Mobile Navigation

### Фаза 2 (Неделя 3-4): Улучшения
- [ ] Упростить дашборды
- [ ] Создать Slide-over компонент
- [ ] Добавить Quick Actions

### Фаза 3 (Неделя 5): Полировка
- [ ] Command Palette
- [ ] Анимации и transitions
- [ ] Тестирование на устройствах

---

**Дата создания:** Декабрь 2024  
**Версия:** 1.0  
**Статус:** Готов к реализации
