# Реализация системы тем (Светлая/Темная)

## ✅ Выполненная работа

### 1. Создана система управления темами

**Файлы:**
- `/contexts/ThemeContext.tsx` - Context для управления темой
- `/components/ThemeToggle.tsx` - Компонент переключателя темы

**Функционал:**
- Автоматическое определение системной темы
- Сохранение выбора в localStorage
- Плавная анимация переключения
- Два варианта кнопки: иконка и компактная

### 2. Настроен Tailwind CSS

**Изменения в `tailwind.config.ts`:**
```typescript
darkMode: 'class' // Включен режим темной темы через класс
```

### 3. Обновлен layout

**Файл: `/app/layout.tsx`**
- Добавлен `ThemeProvider` в корень приложения
- Добавлен `ThemeToggle` в header
- Обновлены классы для поддержки обеих тем

### 4. Обновлена главная страница

**Файл: `/app/page.tsx`**
- Фон: `bg-white dark:bg-premium-black`
- Текст: `text-gray-900 dark:text-white`
- Карточки: `bg-gray-100 dark:bg-white/5`
- Границы: `border-gray-200 dark:border-white/10`

### 5. Обновлены компоненты

**TourCard.tsx:**
- Фон карточки: белый в светлой теме
- Текст: черный/серый в светлой, белый в темной
- Тени и ховер-эффекты

**PartnerCard.tsx:**
- Адаптивный фон и границы
- Читаемый текст в обеих темах

**WeatherWidget.tsx:**
- Темный фон карточки в темной теме
- Адаптивные цвета текста

### 6. Обновлены страницы Hub

**Обновленные страницы:**
- `/app/hub/tour-operator/page.tsx`
- `/app/hub/operator/page.tsx`
- `/app/hub/guide/page.tsx`
- `/app/hub/tourist/page.tsx`

**Изменения:**
- Светлый фон `bg-gray-50` в светлой теме
- Темный фон `dark:bg-premium-black` в темной
- Адаптивные цвета текста и элементов

## 🎨 Цветовая схема

### Светлая тема
- **Фон:** `bg-white`, `bg-gray-50`, `bg-gray-100`
- **Текст:** `text-gray-900`, `text-gray-600`
- **Границы:** `border-gray-200`
- **Акцент:** `text-premium-gold` (остается)

### Темная тема
- **Фон:** `bg-premium-black`, `bg-white/5`
- **Текст:** `text-white`, `text-white/70`
- **Границы:** `border-white/10`
- **Акцент:** `text-premium-gold` (остается)

## 🚀 Использование

### Переключение темы

Пользователь может переключать тему:
1. Через кнопку в header (иконка солнца/луны)
2. Тема автоматически сохраняется в localStorage
3. При первом посещении определяется системная тема

### Для разработчиков

```tsx
import { useTheme } from '@/contexts/ThemeContext';

function MyComponent() {
  const { theme, toggleTheme, setTheme } = useTheme();
  
  return (
    <div className="bg-white dark:bg-gray-800">
      <p className="text-gray-900 dark:text-white">
        Текущая тема: {theme}
      </p>
      <button onClick={toggleTheme}>
        Переключить тему
      </button>
    </div>
  );
}
```

### Классы для адаптивности

Используйте префикс `dark:` для стилей темной темы:

```tsx
// Фон
className="bg-white dark:bg-gray-800"

// Текст
className="text-gray-900 dark:text-white"

// Границы
className="border-gray-200 dark:border-gray-700"

// Ховер-эффекты
className="hover:bg-gray-100 dark:hover:bg-gray-700"
```

## 📝 TODO для полной реализации

- [ ] Обновить оставшиеся страницы hub
- [ ] Добавить анимации переходов
- [ ] Оптимизировать цвета для accessibility (WCAG)
- [ ] Добавить переключатель темы в мобильное меню
- [ ] Создать варианты для кастомных цветов (не только золотой)

## 🎯 Преимущества

✅ **Улучшенный UX** - пользователи выбирают комфортную тему
✅ **Современный дизайн** - соответствует трендам 2024-2025
✅ **Снижение усталости глаз** - темная тема для вечернего использования
✅ **Экономия батареи** - темная тема на OLED экранах
✅ **Accessibility** - лучше для пользователей с чувствительностью к свету
