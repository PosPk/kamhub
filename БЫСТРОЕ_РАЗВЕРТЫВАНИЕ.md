# ⚡ БЫСТРОЕ РАЗВЕРТЫВАНИЕ KAMCHATOUR HUB

> **За 5 минут до production!**  
> Все команды готовы для копирования

---

## 🎯 ТРИ СПОСОБА РАЗВЕРТЫВАНИЯ

### 1️⃣ LOCAL (для разработки) - **5 минут**
### 2️⃣ PRODUCTION (автоматический) - **20 минут**
### 3️⃣ PRODUCTION (быстрый) - **10 минут**

---

## 1️⃣ LOCAL РАЗВЕРТЫВАНИЕ (для разработки)

### Шаг 1: Установить зависимости

```bash
npm install
```

### Шаг 2: Создать .env.local

```bash
cat > .env.local << 'EOF'
# Database (Timeweb Cloud - уже настроено!)
DATABASE_URL="postgresql://gen_user:q;3U+PY7XCz@Br@51e6e5ca5d967b8e81fc9b75.twc1.net:5432/default_db?sslmode=require"
DATABASE_SSL=true

# Node
NODE_ENV=development
PORT=3002

# JWT (для dev)
JWT_SECRET=dev-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# App
NEXT_PUBLIC_APP_URL=http://localhost:3002

# Cron
CRON_SECRET=dev-cron-secret

# API ключи (опционально для dev)
GROQ_API_KEY=
CLOUDPAYMENTS_PUBLIC_ID=
YANDEX_MAPS_API_KEY=
EOF
```

### Шаг 3: Запустить

```bash
npm run dev
```

### Шаг 4: Открыть

```
✅ Сайт:      http://localhost:3002
✅ Дашборд:   http://localhost:3002/hub/tour-operator
✅ API:       http://localhost:3002/api/tours
```

**ГОТОВО! Сайт работает локально! 🎉**

---

## 2️⃣ PRODUCTION РАЗВЕРТЫВАНИЕ (автоматический)

### Требования:
- Timeweb Cloud VDS (2 CPU, 2GB RAM)
- Ubuntu 22.04
- Домен (DNS настроен на IP сервера)

### Шаг 1: Подключиться к серверу

```bash
ssh root@YOUR_SERVER_IP
```

### Шаг 2: Скачать проект

```bash
cd /root
git clone https://github.com/YOUR_REPO/kamchatour-hub.git
cd kamchatour-hub
```

> Если репозитория нет, загрузить через SCP:
```bash
# На локальной машине:
scp -r /workspace root@YOUR_SERVER_IP:/root/kamchatour-hub
```

### Шаг 3: Изменить домен

```bash
nano deploy-production.sh
```

Изменить строку 14:
```bash
DOMAIN="your-domain.com"  # ← ИЗМЕНИТЬ НА ВАШ ДОМЕН!
```

Сохранить: `Ctrl+X`, `Y`, `Enter`

### Шаг 4: Запустить деплой

```bash
chmod +x deploy-production.sh
sudo bash deploy-production.sh
```

### Шаг 5: Подождать (~20 минут)

Скрипт автоматически:
- ✅ Установит Node.js, PM2, Nginx
- ✅ Настроит SSL (Let's Encrypt)
- ✅ Соберет проект
- ✅ Запустит приложение
- ✅ Настроит автозапуск

### Шаг 6: Добавить API ключи

```bash
nano /var/www/kamchatour-hub/.env.production
```

Заполнить:
```bash
GROQ_API_KEY=your_key_here
CLOUDPAYMENTS_PUBLIC_ID=your_id
JWT_SECRET=random-32-chars-secret
CRON_SECRET=random-32-chars-secret
```

### Шаг 7: Рестарт

```bash
pm2 restart kamchatour-hub
```

### Шаг 8: Проверить

```
✅ https://your-domain.com
✅ https://your-domain.com/hub/tour-operator
```

**ГОТОВО! Сайт в production! 🚀**

---

## 3️⃣ PRODUCTION РАЗВЕРТЫВАНИЕ (быстрый)

### Копипаста команд (весь деплой за раз):

```bash
# === ШАГ 1: ПОДКЛЮЧЕНИЕ ===
ssh root@YOUR_SERVER_IP

# === ШАГ 2: УСТАНОВКА NODE.JS ===
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
node -v  # Проверка

# === ШАГ 3: УСТАНОВКА PM2 ===
sudo npm install -g pm2
pm2 -v  # Проверка

# === ШАГ 4: УСТАНОВКА NGINX ===
sudo apt update
sudo apt install -y nginx
sudo systemctl start nginx
sudo systemctl enable nginx

# === ШАГ 5: УСТАНОВКА CERTBOT ===
sudo apt install -y certbot python3-certbot-nginx

# === ШАГ 6: СОЗДАНИЕ ДИРЕКТОРИИ ===
sudo mkdir -p /var/www/kamchatour-hub
cd /var/www/kamchatour-hub

# === ШАГ 7: ЗАГРУЗКА КОДА ===
# Вариант A: Git
git clone https://github.com/YOUR_REPO/kamchatour-hub.git .

# Вариант B: Из локальной машины (запустить на своем компе)
# rsync -avz --exclude 'node_modules' /workspace/ root@YOUR_SERVER_IP:/var/www/kamchatour-hub/

# === ШАГ 8: СОЗДАНИЕ .env.production ===
cat > .env.production << 'EOF'
NODE_ENV=production
PORT=3000
DATABASE_URL="postgresql://gen_user:q;3U+PY7XCz@Br@51e6e5ca5d967b8e81fc9b75.twc1.net:5432/default_db?sslmode=require"
DATABASE_SSL=true
JWT_SECRET=CHANGE_ME_32_CHARS_MINIMUM
JWT_EXPIRES_IN=7d
NEXT_PUBLIC_APP_URL=https://YOUR_DOMAIN.com
CRON_SECRET=CHANGE_ME_32_CHARS_MINIMUM
GROQ_API_KEY=
CLOUDPAYMENTS_PUBLIC_ID=
CLOUDPAYMENTS_API_SECRET=
EOF

# === ШАГ 9: СБОРКА ===
npm ci
npm run build
npm prune --production

# === ШАГ 10: PM2 КОНФИГ ===
cat > ecosystem.config.js << 'EOF'
module.exports = {
  apps: [{
    name: 'kamchatour-hub',
    script: 'npm',
    args: 'start',
    instances: 2,
    exec_mode: 'cluster',
    env: { NODE_ENV: 'production', PORT: 3000 }
  }]
};
EOF

# === ШАГ 11: ЗАПУСК PM2 ===
mkdir -p logs
pm2 start ecosystem.config.js
pm2 startup systemd
pm2 save

# === ШАГ 12: NGINX КОНФИГ ===
cat > /tmp/kamchatour-hub << 'EOF'
server {
    listen 80;
    server_name YOUR_DOMAIN.com www.YOUR_DOMAIN.com;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
EOF

sudo mv /tmp/kamchatour-hub /etc/nginx/sites-available/
sudo ln -sf /etc/nginx/sites-available/kamchatour-hub /etc/nginx/sites-enabled/
sudo rm -f /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl restart nginx

# === ШАГ 13: SSL ===
sudo certbot --nginx -d YOUR_DOMAIN.com -d www.YOUR_DOMAIN.com

# === ШАГ 14: FIREWALL ===
sudo ufw allow ssh
sudo ufw allow 'Nginx Full'
sudo ufw --force enable

# === ШАГ 15: CRON ===
(crontab -l 2>/dev/null; echo "* * * * * curl -H 'X-Cron-Secret: CHANGE_ME' https://YOUR_DOMAIN.com/api/cron/cleanup-holds") | crontab -

# === ГОТОВО! ===
echo "✅ Деплой завершен!"
echo "🌐 Сайт: https://YOUR_DOMAIN.com"
pm2 status
```

**⚠️ ВАЖНО:** Замените:
- `YOUR_SERVER_IP`
- `YOUR_DOMAIN.com`
- `YOUR_REPO`
- `CHANGE_ME` (JWT_SECRET, CRON_SECRET)

---

## ✅ ПРОВЕРКА ПОСЛЕ ДЕПЛОЯ

### 1. Проверить PM2

```bash
pm2 status
pm2 logs kamchatour-hub --lines 50
```

Ожидаем:
```
┌─────┬───────────────────┬─────────┬───────┐
│ id  │ name              │ status  │ cpu   │
├─────┼───────────────────┼─────────┼───────┤
│ 0   │ kamchatour-hub    │ online  │ 0%    │
│ 1   │ kamchatour-hub    │ online  │ 0%    │
└─────┴───────────────────┴─────────┴───────┘
```

### 2. Проверить Nginx

```bash
sudo nginx -t
sudo systemctl status nginx
```

### 3. Проверить порты

```bash
sudo netstat -tlnp | grep -E ':(80|443|3000)'
```

Ожидаем:
```
tcp  0  0  0.0.0.0:80     0.0.0.0:*  LISTEN  nginx
tcp  0  0  0.0.0.0:443    0.0.0.0:*  LISTEN  nginx
tcp  0  0  127.0.0.1:3000 0.0.0.0:*  LISTEN  node
```

### 4. Проверить сайт

```bash
# Локально на сервере
curl http://localhost:3000

# По домену
curl https://your-domain.com
```

### 5. Проверить API

```bash
curl https://your-domain.com/api/health/db
```

Ожидаем:
```json
{
  "status": "ok",
  "database": "connected"
}
```

### 6. Проверить дашборд

```bash
# В браузере:
https://your-domain.com/hub/tour-operator
```

---

## 🔧 КОМАНДЫ УПРАВЛЕНИЯ

### PM2:

```bash
pm2 status                     # Статус
pm2 logs kamchatour-hub        # Логи
pm2 restart kamchatour-hub     # Рестарт
pm2 stop kamchatour-hub        # Остановка
pm2 delete kamchatour-hub      # Удаление
pm2 monit                      # Мониторинг
```

### Nginx:

```bash
sudo systemctl status nginx    # Статус
sudo systemctl restart nginx   # Рестарт
sudo systemctl reload nginx    # Перезагрузка конфига
sudo nginx -t                  # Проверка конфига
```

### Логи:

```bash
# PM2 логи
pm2 logs kamchatour-hub --lines 100

# Nginx логи
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log
```

### Обновление кода:

```bash
cd /var/www/kamchatour-hub
git pull origin main
npm install
npm run build
pm2 restart kamchatour-hub
```

---

## 🎯 ПОЛУЧЕНИЕ API КЛЮЧЕЙ

### 1. GROQ API (AI чат) - БЕСПЛАТНО

```
1. Зайти: https://console.groq.com
2. Sign up (Google аккаунт)
3. Create API Key
4. Скопировать: gsk_...
5. Добавить в .env.production
```

### 2. CloudPayments (Платежи)

```
1. Зайти: https://cloudpayments.ru
2. Зарегистрироваться
3. Получить Public ID и API Secret
4. Добавить в .env.production
```

### 3. Yandex Maps (Карты) - БЕСПЛАТНО

```
1. Зайти: https://developer.tech.yandex.ru
2. Создать приложение
3. Получить API ключ
4. Добавить в .env.production
```

### 4. Gmail SMTP (Email)

```
1. Включить 2FA в Google аккаунте
2. Создать App Password:
   Google Account → Security → 2-Step Verification → App passwords
3. Добавить в .env.production:
   SMTP_USER=your-email@gmail.com
   SMTP_PASS=app-password
```

### 5. SMS.ru (SMS) - ОПЦИОНАЛЬНО

```
1. Зайти: https://sms.ru
2. Зарегистрироваться
3. Получить API ID
4. Добавить в .env.production
```

---

## 🐛 БЫСТРЫЙ FIX

### Сайт не работает?

```bash
# Рестарт всего
pm2 restart kamchatour-hub
sudo systemctl restart nginx

# Проверить логи
pm2 logs kamchatour-hub --err
sudo tail -f /var/log/nginx/error.log
```

### 502 Bad Gateway?

```bash
# Проверить что Next.js запущен
curl http://localhost:3000

# Если не отвечает
pm2 restart kamchatour-hub
pm2 logs kamchatour-hub
```

### SSL не работает?

```bash
# Проверить сертификат
sudo certbot certificates

# Пересоздать
sudo certbot --nginx -d your-domain.com --force-renewal
```

### Database connection error?

```bash
# Проверить DATABASE_URL
cat /var/www/kamchatour-hub/.env.production | grep DATABASE_URL

# Проверить подключение
psql 'postgresql://gen_user:q;3U+PY7XCz@Br@51e6e5ca5d967b8e81fc9b75.twc1.net:5432/default_db?sslmode=require'
```

---

## 📊 CHECKLIST ДЕПЛОЯ

### Перед деплоем:
- [ ] VDS создан (Timeweb Cloud, 2GB RAM)
- [ ] Домен куплен
- [ ] DNS настроен (A-запись на IP)
- [ ] SSH доступ работает

### Во время деплоя:
- [ ] Node.js установлен
- [ ] PM2 установлен
- [ ] Nginx установлен
- [ ] Код загружен
- [ ] .env.production создан
- [ ] Проект собран
- [ ] PM2 запущен
- [ ] Nginx настроен
- [ ] SSL получен

### После деплоя:
- [ ] Сайт открывается
- [ ] API работает
- [ ] Дашборд открывается
- [ ] Database подключена
- [ ] API ключи добавлены
- [ ] Логи без ошибок
- [ ] Cron работает
- [ ] SSL сертификат валиден

---

## 🎉 РЕЗУЛЬТАТ

После деплоя у вас будет:

✅ **Полноценный сайт** на вашем домене  
✅ **HTTPS** с автообновлением сертификата  
✅ **PM2** с cluster mode (2 процесса)  
✅ **Nginx** с reverse proxy и gzip  
✅ **30+ таблиц** в PostgreSQL  
✅ **28 API endpoints**  
✅ **Дашборд оператора** с real-time данными  
✅ **Автоматический cleanup** (cron)  
✅ **Автозапуск** при перезагрузке  

---

## 🚀 БЫСТРЫЕ ССЫЛКИ

**Local dev:**
```bash
npm run dev
http://localhost:3002
```

**Production:**
```bash
ssh root@your-server-ip
pm2 status
https://your-domain.com
```

**Дашборд:**
```bash
https://your-domain.com/hub/tour-operator
```

**API:**
```bash
https://your-domain.com/api/tours
https://your-domain.com/api/tours/operator/dashboard?operatorId=xxx
```

---

**Готово к деплою! Выбирайте способ и вперед! 🚀**
