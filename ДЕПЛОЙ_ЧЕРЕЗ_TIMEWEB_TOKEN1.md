# 🚀 ДЕПЛОЙ ЧЕРЕЗ TIMEWEB_TOKEN1

**Используется:** `TIMEWEB_TOKEN1` из GitHub Secrets  
**Метод:** Автоматический деплой через Timeweb Cloud API + SSH

---

## ✅ ЧТО НАСТРОЕНО

### 1. GitHub Actions Workflows:

#### `.github/workflows/timeweb-api-deploy.yml` ⭐ ГЛАВНЫЙ
- Использует `TIMEWEB_TOKEN1` для получения информации о сервере через API
- Автоматически находит IP сервера
- Выполняет деплой через SSH

#### `.github/workflows/timeweb-api-auto.yml` ⚡ БЫСТРЫЙ
- Упрощенный workflow для быстрого деплоя
- Автоматически находит сервер через API
- Минимальная конфигурация

#### `.github/workflows/auto-deploy.yml` 🔄 ОБНОВЛЕН
- Теперь также использует `TIMEWEB_TOKEN1` для получения IP
- Умный поиск сервера через API

---

## 🔐 НУЖНЫЕ GITHUB SECRETS

### Обязательные:

| Секрет | Описание | Откуда взять |
|--------|----------|--------------|
| `TIMEWEB_TOKEN1` | ⭐ API токен Timeweb | https://timeweb.cloud/my/api |
| `TIMEWEB_SSH_PASSWORD` | SSH пароль | Ваш пароль (xQvB1pv?yZTjaR) |

### Опциональные:

| Секрет | Описание | По умолчанию |
|--------|----------|--------------|
| `TIMEWEB_SSH_USER` | SSH пользователь | `root` |
| `TIMEWEB_SSH_PORT` | SSH порт | `22` |
| `DATABASE_URL` | URL базы данных | Из настроек |
| `NEXT_PUBLIC_APP_URL` | URL приложения | `https://kamchatour.timeweb.app` |

---

## 🚀 КАК РАБОТАЕТ

### Автоматический процесс:

1. **Push в репозиторий** → запускается workflow
2. **Получение IP через API** → использует `TIMEWEB_TOKEN1` для запроса к Timeweb API
3. **Поиск сервера** → автоматически находит первый доступный сервер
4. **SSH подключение** → использует найденный IP и пароль из секретов
5. **Деплой** → обновление кода, сборка, миграции, перезапуск

---

## 📋 БЫСТРЫЙ СТАРТ

### Шаг 1: Добавить GitHub Secrets

Откройте:
```
https://github.com/ВАШ_USERNAME/kamhub/settings/secrets/actions
```

Добавьте:
- `TIMEWEB_TOKEN1` = ваш токен из https://timeweb.cloud/my/api
- `TIMEWEB_SSH_PASSWORD` = `xQvB1pv?yZTjaR`

### Шаг 2: Сделать push

```bash
git push origin main
```

**Всё!** Workflow автоматически:
- ✅ Найдет сервер через API
- ✅ Задеплоит приложение
- ✅ Проверит здоровье

---

## 🔍 РУЧНОЙ ЗАПУСК

### Настройка инфраструктуры:

```bash
# В GitHub Actions выберите:
Actions → timeweb-api-deploy → Run workflow → action: setup
```

### Проверка статуса:

```bash
# В GitHub Actions выберите:
Actions → timeweb-api-deploy → Run workflow → action: status
```

### Деплой:

```bash
# В GitHub Actions выберите:
Actions → timeweb-api-deploy → Run workflow → action: deploy
```

Или просто:
```bash
git push origin main
```

---

## 🎯 ПРЕИМУЩЕСТВА

### ✅ Автоматическое обнаружение сервера
- Не нужно указывать IP вручную
- Workflow сам найдет сервер через API

### ✅ Использование Timeweb API
- Полная интеграция с Timeweb Cloud
- Возможность управления инфраструктурой

### ✅ Гибкость
- Работает с существующими серверами
- Автоматически находит первый доступный

---

## 📊 КАК ЭТО РАБОТАЕТ

### Workflow выполняет:

```yaml
1. Checkout code
2. Setup Node.js
3. Install dependencies
4. Получение IP через API (TIMEWEB_TOKEN1)
   └─ curl -H "Authorization: Bearer $TIMEWEB_TOKEN1" \
      https://api.timeweb.cloud/api/v1/servers
   └─ Извлечение IP из ответа
5. SSH деплой на найденный IP
   └─ Обновление кода
   └─ npm ci
   └─ npm run build
   └─ npm run migrate:up
   └─ pm2 restart
6. Health check
```

---

## 🐛 TROUBLESHOOTING

### TIMEWEB_TOKEN1 не найден:
```
❌ TIMEWEB_TOKEN1 не найден в GitHub Secrets!
```
**Решение:** Добавьте секрет `TIMEWEB_TOKEN1` в GitHub

### Сервер не найден через API:
```
⚠️ Серверы не найдены через API, используем прямой SSH деплой
```
**Решение:** Workflow автоматически использует IP из `TIMEWEB_SSH_HOST` или `5.129.248.224`

### SSH подключение не работает:
```
❌ Connection failed
```
**Решение:** Проверьте `TIMEWEB_SSH_PASSWORD` в секретах

---

## ✅ ГОТОВО!

**Все workflows настроены для использования `TIMEWEB_TOKEN1`!**

**Осталось только:**
1. Добавить `TIMEWEB_TOKEN1` в GitHub Secrets
2. Добавить `TIMEWEB_SSH_PASSWORD` (если еще не добавлен)
3. Сделать push

**После этого деплой будет полностью автоматическим!** 🚀

---

## 📝 ПРИМЕЧАНИЯ

- Workflow автоматически находит сервер через API
- Если API не доступен, используется прямой SSH деплой
- Все процессы автоматизированы
- Health check выполняется после деплоя

**Начните с добавления `TIMEWEB_TOKEN1` в GitHub Secrets!** 🔐
