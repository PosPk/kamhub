# Kamchatour Hub - Test Environment Configuration
# Используется для unit и integration тестов

# Test Database
# ВАЖНО: Используйте отдельную тестовую БД, не production!
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/kamchatour_hub_test"
DATABASE_SSL=false
DATABASE_MAX_CONNECTIONS=5

# Node Environment
NODE_ENV=test
PORT=3003

# JWT (тестовые значения)
JWT_SECRET=test-secret-key-do-not-use-in-production
JWT_EXPIRES_IN=1h

# Application
NEXT_PUBLIC_APP_URL=http://localhost:3003

# AI APIs (mock для тестов)
GROQ_API_KEY=test-groq-key
DEEPSEEK_API_KEY=test-deepseek-key
OPENROUTER_API_KEY=test-openrouter-key

# Payments (mock для тестов)
CLOUDPAYMENTS_PUBLIC_ID=test-public-id
CLOUDPAYMENTS_API_SECRET=test-secret-key

# Maps (mock для тестов)
YANDEX_MAPS_API_KEY=test-maps-key
YANDEX_WEATHER_API_KEY=test-weather-key

# Notifications (mock для тестов)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=test
SMTP_PASS=test
EMAIL_FROM=test@kamchatour.ru

# SMS (mock для тестов)
SMS_RU_API_ID=test-sms-id

# Telegram (mock для тестов)
TELEGRAM_BOT_TOKEN=test-bot-token
TELEGRAM_CHAT_ID=test-chat-id

# Monitoring (отключено для тестов)
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
