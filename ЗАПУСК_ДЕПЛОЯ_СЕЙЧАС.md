# 🚀 ЗАПУСК ДЕПЛОЯ ПРЯМО СЕЙЧАС

**Статус:** ✅ ВСЁ ГОТОВО! Секреты настроены, workflows готовы!

---

## ⚡ БЫСТРЫЙ ЗАПУСК (3 СПОСОБА)

### Способ 1: Push в main (самый простой)

```bash
git add .
git commit -m "Ready for deployment"
git push origin main
```

**Автоматически запустится:**
- `timeweb-api-auto.yml` ⚡
- `auto-deploy.yml` 🔄

---

### Способ 2: Ручной запуск через GitHub Actions

1. Откройте:
   ```
   https://github.com/ВАШ_USERNAME/kamhub/actions
   ```

2. Выберите workflow:
   - **`timeweb-api-deploy`** ⭐ (полный функционал)
   - **`timeweb-api-auto`** ⚡ (быстрый)
   - **`Auto Deploy via Timeweb API`** (автоматический)

3. Нажмите **"Run workflow"**

4. Выберите действие:
   - `setup` - настройка инфраструктуры
   - `deploy` - деплой приложения
   - `status` - проверка статуса

5. Нажмите **"Run workflow"**

---

### Способ 3: Через GitHub CLI (если установлен)

```bash
gh workflow run timeweb-api-deploy.yml --ref main
```

---

## 📋 ЧТО БУДЕТ ПРОИСХОДИТЬ

### Автоматический процесс:

1. ✅ Получение `TIMEWEB_TOKEN1` из секретов
2. ✅ Запрос к Timeweb API: `GET /api/v1/servers`
3. ✅ Поиск первого доступного сервера
4. ✅ Извлечение IP адреса
5. ✅ SSH подключение (используя пароль из секретов)
6. ✅ Обновление кода из Git
7. ✅ Установка зависимостей (`npm ci`)
8. ✅ Проверка типов (`npm run type-check`)
9. ✅ Сборка приложения (`npm run build`)
10. ✅ Применение миграций (`npm run migrate:up`)
11. ✅ Перезапуск через PM2
12. ✅ Health check

---

## 🎯 КАКОЙ WORKFLOW ВЫБРАТЬ?

| Workflow | Когда использовать | Особенности |
|----------|-------------------|-------------|
| **`timeweb-api-auto`** ⚡ | Быстрый деплой | Автоматический, минимальная конфигурация |
| **`timeweb-api-deploy`** ⭐ | Полный контроль | Setup/Deploy/Status, ручной выбор действий |
| **`auto-deploy`** 🔄 | Универсальный | С интеграцией API, fallback на SSH |

**Рекомендация:** Используйте `timeweb-api-auto` для автоматического деплоя!

---

## ✅ ПРОВЕРКА ПЕРЕД ЗАПУСКОМ

### Секреты должны быть:

- ✅ `TIMEWEB_TOKEN1` - API токен Timeweb
- ✅ `TIMEWEB_SSH_PASSWORD` - SSH пароль
- ✅ `TIMEWEB_SSH_USER` (опционально, по умолчанию `root`)

### Проверить секреты:

```
https://github.com/ВАШ_USERNAME/kamhub/settings/secrets/actions
```

---

## 🚀 ЗАПУСК ПРЯМО СЕЙЧАС

### Вариант 1: Через push (рекомендуется)

```bash
# В терминале проекта:
git add .
git commit -m "Deploy to Timeweb via API"
git push origin main
```

### Вариант 2: Через GitHub Actions UI

1. https://github.com/ВАШ_USERNAME/kamhub/actions
2. Нажмите на `⚡ Auto Deploy via Timeweb API`
3. Нажмите **"Run workflow"** → **"Run workflow"**

---

## 📊 МОНИТОРИНГ ДЕПЛОЯ

### Проверить статус:

```
https://github.com/ВАШ_USERNAME/kamhub/actions
```

### Просмотр логов:

1. Откройте нужный workflow run
2. Нажмите на job (например, "🚀 Автоматический деплой")
3. Смотрите логи каждого шага

---

## 🎉 ПОСЛЕ ЗАПУСКА

### Проверить работу приложения:

```bash
# После деплоя workflow автоматически найдет IP и покажет его
# Или проверьте в логах workflow

curl http://НАЙДЕННЫЙ_IP:3000/api/health
```

### Проверить на сервере:

```bash
ssh root@НАЙДЕННЫЙ_IP
pm2 list
pm2 logs kamchatour-hub
```

---

## 🐛 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

### Workflow не запускается:
- Проверьте что файлы в `.github/workflows/` закоммичены
- Убедитесь что ветка `main` выбрана

### Ошибка "TIMEWEB_TOKEN1 not found":
- Проверьте что секрет называется именно `TIMEWEB_TOKEN1`
- Убедитесь что он добавлен в репозиторий

### Ошибка подключения к серверу:
- Проверьте `TIMEWEB_SSH_PASSWORD` в секретах
- Убедитесь что сервер доступен

### Сервер не найден через API:
- Workflow автоматически использует fallback на прямой SSH
- Проверьте что `TIMEWEB_SSH_HOST` добавлен в секреты (или используется `5.129.248.224`)

---

## ✅ ВСЁ ГОТОВО!

**Секреты настроены ✅**  
**Workflows готовы ✅**  
**Осталось только запустить!** 🚀

---

## 🎯 РЕКОМЕНДАЦИЯ

**Просто сделайте:**
```bash
git push origin main
```

**Или запустите вручную через GitHub Actions UI!** 🚀

---

**УДАЧНОГО ДЕПЛОЯ!** 🎉
